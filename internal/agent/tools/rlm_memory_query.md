Query the hypergraph memory for stored knowledge.

Returns memory nodes matching the query, including:
- Node type (fact, experience, decision, etc.)
- Content
- Confidence score
- Memory tier (task, session, long-term)
- Timestamp

Use this to inspect what knowledge has been stored in the RLM memory system.
